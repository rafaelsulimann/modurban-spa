import HeaderInfos from "../HeaderInfos";
import HeaderNav from "../HeaderNav";

export default function Header() {
  return (
    <header id="header">
      <HeaderInfos />
      <HeaderNav />
    </header>
  );
}
